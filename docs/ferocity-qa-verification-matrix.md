# Ferocity QA Verification Matrix

This matrix tracks the hardening pass requested before the next deploy. Do not mark a workflow `Pass` unless the full path has been verified with automated and manual checks.

| Workflow | Previous failure | Root cause | Fix | Automated test | Manual verification | Final status |
| --- | --- | --- | --- | --- | --- | --- |
| Production app errors | 54 app-error events looked like unknown crashes | 52 were Stripe webhook signature failures; 1 Resend unmatched route; 1 Google placeholder call | Grouped error page, redacted metadata, correlation IDs, safe categories, retryability | 124 tests, typecheck, lint, production build, and production-readiness checks pass | Grouping inspected without exposing secrets; provider-independent webhook contracts pass | Pass |
| Login | Buttons could look inert during submit | Pending state was not visually/semantically consistent | Shared `ActionStatusButton` on login paths | Authenticated/unauthenticated route crawl passed 122 checks | QA login and protected navigation completed in isolated browser | Pass |
| Lead scoring | Score action had no clear success/failure receipt | Server action returned void and failures were hard to see | Stateful form, user-facing message, correlation ID on failure, focus moves to result | Full test and build suite passes | QA lead scored 85/100 (`hot`) with reasons and success feedback | Pass |
| Operational QA | QA run had weak submit feedback and duplicate click risk | Server action returned void; QA inserts were not idempotent | Stateful form, short-window idempotent insert, correlation ID failures | Operational and customer-path smoke tests pass | QA run saved once and accurately reported the intentionally incomplete demo workspace checks | Pass |
| Revenue scanning | Scan action had weak feedback and repeated scans could duplicate workspace recommendations | Nullable `brand_id` bypassed the original unique constraint | Stateful receipt plus partial unique index and conflict target for workspace recommendations | Migration 143, tests, migration coverage, and production build pass | Repeated browser scans remain at four recommendations instead of doubling | Pass |
| AI commands | User could expect immediate mutation from plain language | Read-only vs action intent was not explicit | Intent classifier, read-only redirects, apply flow stays reviewed | Command-intent and AI-command smoke tests pass | “Show me what needs attention today” routed to the owner attention view | Pass |
| Estimate/customer/job/invoice creation | Repeated clicks or cross-workspace customer IDs could create bad records | Mutations relied on workspace-scoped inserts but did not explicitly verify customer ownership first | Added customer workspace guard and recent duplicate checks | Safety, estimate-path, customer-path, and office-manager smoke tests pass | Authenticated customer path rendered successfully | Pass |
| Payment recording/webhooks | Retried payment events could risk duplicate accounting | Stripe conflict path could return an existing payment row; manual payment lacked fast duplicate guard | Stripe duplicates do nothing before invoice update; manual payment has 45-second duplicate guard | `service/actions.safety.test.ts` passed | Needs staging Stripe webhook retry test | Partial |
| Workspace isolation | Needs proof one workspace cannot reach another | RLS exists, but server action regression needed | Added service-action workspace guards and sensitive-table grant checks | `db:verify-rls` passes tenant coverage and sensitive-table grants | Verified against the connected database | Pass |
| Observability | Failures did not always give a safe reference | Logging did not return a correlation ID | `safeLogAppError`, metadata redaction, route/action categories | Tests, typecheck, lint, and production build pass | Error grouping and safe references inspected | Pass |
| Accessibility | Result/error focus was weak on new forms | Feedback panels were passive status text | Result panels use alert/status and focus after submit | Lint, UI guard, and production build pass | Lead, QA, and revenue forms exposed active status feedback; phone forms have labeled controls | Pass |
| Responsive navigation | Mobile app nav required horizontal scrolling | CSS forced nowrap/overflow-x on app nav | App nav wraps; data tables still scroll where appropriate | UI guard and production build pass | Phone setup and Office Manager inspected at 390×844 and 1440×1000 with no horizontal overflow | Pass |
| Repeatable QA seed | No single resettable seed for full customer path | Seed covered login basics only | Added repeatable seed and removed duplicate fake QA leads safely | Seed script and full release suite pass | Rerun leaves one exact fake QA lead in the QA tenant | Pass |
| Provider-independent phone setup | Telecom jargon and provider choice could leak into normal onboarding | Setup was centered on implementation providers rather than the owner's number | Four customer paths persist independently of phone/voice adapters; technical controls stay under Advanced | Phone contracts, receptionist smoke, 124 tests, and production build pass | Forwarding, full integration, new number, and BYO provider were each saved and reloaded | Pass |
| Industry-specific phone AI | Voice behavior needed to vary by industry and business without coupling to a vendor | Provider profiles did not share one canonical business prompt | Added a provider-neutral voice profile builder using industry knowledge plus workspace name, greeting, tone, languages, goals, instructions, escalation rules, and guardrails | Voice profile unit tests and office-manager smoke pass | Custom QA profile saved, reported success, and persisted after reload | Pass |
